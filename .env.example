# API base URL for photo uploads (Worker).
# Production (Cloudflare): use Workers URL or custom domain.
# Local dev: use http://localhost:8787 when running `npm run worker:dev`.
VITE_API_URL=https://family-tree-app.eugene-tawiah.workers.dev
